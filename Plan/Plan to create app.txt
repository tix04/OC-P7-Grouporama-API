==============User steps

1 - Land on homepage
if(user doesn't have an account)
    a - Clicks on Sign up button
    b - Fill in form
        ======Form Requirements
        * All Fields except for Age are required
        * No empty strings '' or ' ' in text Fields
        * email must follow correct format
        * Names are turned to lowercase when sent to database
        * User notified if any of these restrictions occur
    c - When user properly creates an account: form submitted
        * record of user is created in SQL database with the correct data formats in each Fields
        * Find a method to check if user is connected or not. Verify user credentials in DB and compare to user input
        * User automatically sent to page where all posts are rendered
        * Something needs to notify user they are coonected visually

if(user)